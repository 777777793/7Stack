---
import { getCollection } from 'astro:content';
import ArticleCard from '../../components/ArticleCard.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getEntryUrl, isPublished, sortEntriesByDate } from '../../lib/content';

const posts = await getCollection('posts', isPublished);
const entries = sortEntriesByDate(posts);
---

<BaseLayout title="全部文章" description="所有项目复盘和阅读笔记。">
  <div class="mb-xl">
    <a href="/" class="inline-flex items-center gap-2 text-label-md font-medium text-secondary hover:text-on-secondary-fixed-variant transition-colors group">
      <svg class="w-4 h-4 transition-transform group-hover:-translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
      返回首页
    </a>
  </div>

  <header class="mb-xl text-center max-w-3xl mx-auto pt-lg">
    <p class="text-label-sm font-medium text-secondary uppercase tracking-widest mb-2">All Posts</p>
    <h1 class="text-headline-xxl font-semibold text-primary tracking-tight">全部文章</h1>
    <p class="text-body-lg text-on-surface-variant max-w-2xl mx-auto mt-2 leading-relaxed">项目复盘和阅读笔记。面试题专题单独放在复习室里。</p>
  </header>

  <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-gutter">
    {entries.map((entry) => <ArticleCard post={entry} href={getEntryUrl(entry)} />)}
  </section>
</BaseLayout>
